package services;

import org.assertj.core.api.AbstractFileAssert;
import org.assertj.core.util.Arrays;
import org.testng.Assert;
import org.testng.annotations.Test;

import java.util.List;
import java.util.Objects;

import static org.assertj.core.api.FactoryBasedNavigableListAssert.assertThat;

public class NamesTest {
    @Test
    public void testNames(){
       // Names nameList = new Names("Jaś","Małgosia", "Wilk", "Gajowy", "głodny Troll");
      //  List<String> posortowane = nameList.getnames();

       // List<String> oczekiwane = List.of("Gajowy","głodny Troll", "Jaś", "Małgosia", "Wilk");
        //assertThat(posortowane)
       //         .hasSize(5)
       //         .isSorted();
      //  assertThat(posortowane).isEqualTo(oczekiwane);
    }



}
